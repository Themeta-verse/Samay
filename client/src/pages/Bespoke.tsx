/* Atelier Obsidian: bespoke is a tactile selection conversation, never a dashboard and never a false transaction. */
import { FormEvent, useState } from "react";
import { ArrowUpRight, Check } from "lucide-react";
import { Link } from "wouter";
import { ArrowLink, SectionLabel } from "@/components/SiteShell";
import { watches } from "@/lib/samayData";
import { trpc } from "@/lib/trpc";

const caseOptions = [["steel", "Brushed steel", "The house standard"], ["titanium", "Bead-blasted titanium", "Weight, removed"], ["gold", "Champagne gold", "A measured accent"]] as const;
const dialOptions = [["ivory", "Warm ivory"], ["slate", "Slate grey"], ["charcoal", "Charcoal"]] as const;
const strapOptions = [["leather", "Calfskin"], ["steel", "Articulated steel"], ["rubber", "Textured rubber"]] as const;

export default function Bespoke() {
  const [watch, setWatch] = useState(watches[0].slug);
  const [caseChoice, setCaseChoice] = useState("steel");
  const [dialChoice, setDialChoice] = useState("ivory");
  const [strapChoice, setStrapChoice] = useState("leather");
  const [requested, setRequested] = useState(false);
  const [error, setError] = useState("");
  const [delivery, setDelivery] = useState<"sent" | "not_configured">("not_configured");
  const selected = watches.find((item) => item.slug === watch) ?? watches[0];
  const { data: appointment } = trpc.appointment.availability.useQuery();
  const requestMutation = trpc.concierge.submit.useMutation({ onSuccess: (result) => { setDelivery(result.delivery === "sent" ? "sent" : "not_configured"); setRequested(true); }, onError: () => setError("The concierge channel could not prepare this request. Please try again later.") });
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); const name = String(form.get("name") ?? "").trim(); const email = String(form.get("email") ?? "").trim(); if (!name || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) { setError("Please include your name and a valid email address."); return; } setError(""); requestMutation.mutate({ kind: "bespoke_selection", name, email, reference: selected.name, message: String(form.get("occasion") ?? "").trim() || undefined, selection: { case: caseChoice, dial: dialChoice, strap: strapChoice, occasion: String(form.get("occasion") ?? "").trim() || undefined } }); };
  return <div className="flagship-page bespoke-page"><section className="bespoke-hero section-dark"><div className="section-wrap"><SectionLabel index="01" dark>Private selection</SectionLabel><div className="bespoke-hero__grid"><div><h1>Make it<br /><em>your hours.</em></h1><p>A quiet configuration conversation for the watch, material, and occasion that stay with you.</p></div><div className={`bespoke-preview bespoke-preview--${caseChoice}-${dialChoice}-${strapChoice}`}><img src={selected.image} alt={`${selected.name} preview`} /><span>Preview / {selected.reference}</span></div></div></div></section><section className="bespoke-workbench section-paper"><div className="section-wrap"><SectionLabel index="02">Select the object</SectionLabel><div className="bespoke-workbench__grid"><div className="bespoke-controls"><label className="select-label">Reference<select value={watch} onChange={(event) => setWatch(event.target.value)}>{watches.map((item) => <option key={item.slug} value={item.slug}>{item.name} / {item.family}</option>)}</select></label><fieldset><legend>Case</legend><div className="option-list">{caseOptions.map(([value, label, note]) => <button type="button" key={value} className={caseChoice === value ? "is-active" : ""} onClick={() => setCaseChoice(value)} aria-pressed={caseChoice === value}><span className={`option-swatch option-swatch--${value}`} /><span><strong>{label}</strong><small>{note}</small></span><ArrowUpRight size={15} strokeWidth={1.1} /></button>)}</div></fieldset><fieldset><legend>Dial</legend><div className="chip-list">{dialOptions.map(([value, label]) => <button type="button" key={value} className={dialChoice === value ? "is-active" : ""} onClick={() => setDialChoice(value)} aria-pressed={dialChoice === value}>{label}</button>)}</div></fieldset><fieldset><legend>Strap / bracelet</legend><div className="chip-list">{strapOptions.map(([value, label]) => <button type="button" key={value} className={strapChoice === value ? "is-active" : ""} onClick={() => setStrapChoice(value)} aria-pressed={strapChoice === value}>{label}</button>)}</div></fieldset></div><div className="bespoke-summary"><span className="eyebrow">Your selection</span><h2>{selected.name}<br /><em>{selected.short}</em></h2><div className="bespoke-summary__line"><span>Case</span><strong>{caseOptions.find(([value]) => value === caseChoice)?.[1]}</strong></div><div className="bespoke-summary__line"><span>Dial</span><strong>{dialOptions.find(([value]) => value === dialChoice)?.[1]}</strong></div><div className="bespoke-summary__line"><span>Strap</span><strong>{strapOptions.find(([value]) => value === strapChoice)?.[1]}</strong></div><p>This selection is a private starting point. No payment is taken, and no order is implied.</p><ArrowLink href={`/watch/${selected.slug}`}>Examine the reference</ArrowLink></div></div></div></section><section className="bespoke-request section-dark"><div className="section-wrap">{requested ? <div className="request-confirmation" aria-live="polite"><div className="request-confirmation__mark"><Check size={22} strokeWidth={1.1} /></div><span className="eyebrow">{delivery === "sent" ? "Conversation received" : "Conversation prepared"}</span><h2>A starting point<br /><em>for review.</em></h2><p>{delivery === "sent" ? `Your ${selected.name} selection has been shared with the SAMAY concierge for review.` : `Your ${selected.name} selection was checked and prepared in this browser. It was not sent, stored, or shared because a secure concierge destination has not yet been configured.`}</p>{appointment?.isActive && appointment.bookingPageUrl ? <a className="button-primary request-confirmation__appointment" href={appointment.bookingPageUrl} target="_blank" rel="noreferrer">Choose a viewing time <ArrowUpRight size={15} strokeWidth={1.2} /></a> : null}{delivery === "not_configured" ? <Link href="/contact" className="arrow-link arrow-link--dark"><span>Contact the house</span><ArrowUpRight size={15} strokeWidth={1.2} /></Link> : null}<Link href="/inquiry" className="arrow-link arrow-link--dark"><span>Continue to private viewing</span><ArrowUpRight size={15} strokeWidth={1.2} /></Link></div> : <form className="bespoke-request__grid" onSubmit={submit} noValidate><div><SectionLabel index="03" dark>Continue by conversation</SectionLabel><h2>A private<br /><em>starting point.</em></h2><p>Share your preference and the occasion around it. Your selection remains visible here while you prepare the conversation.</p></div><div className="bespoke-request__form"><label><span>Name</span><input name="name" placeholder="Your name" autoComplete="name" /></label><label><span>Email</span><input type="email" name="email" placeholder="Your email address" autoComplete="email" /></label><label><span>Occasion</span><input name="occasion" placeholder="A birthday, a new chapter, simply because" /></label><aside className="form-handoff form-handoff--dark"><span>Appointment availability</span><p>{appointment?.isActive ? "The Google Calendar private-viewing schedule is available after your request is prepared." : "The house can be reached directly while the Google Calendar viewing schedule is being prepared."}</p></aside><aside className="form-handoff form-handoff--dark"><span>Concierge handoff</span><p>Your selection is validated and can be delivered only through an approved secure concierge channel. Until that channel is configured, no request data is sent, stored, or shared.</p></aside>{error && <p className="form-error form-error--dark" role="alert">{error}</p>}<button type="submit" className="button-primary" disabled={requestMutation.isPending}>{requestMutation.isPending ? "Preparing consultation" : "Prepare consultation"} <ArrowUpRight size={15} strokeWidth={1.1} /></button></div></form>}</div></section></div>;
}
