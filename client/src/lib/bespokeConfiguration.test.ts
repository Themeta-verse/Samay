import{describe,expect,it}from"vitest";import{configurationId,defaultBespokeConfiguration,restoreBespokeConfiguration}from"./bespokeConfiguration";
describe("bespoke configuration",()=>{it("creates deterministic IDs",()=>{expect(configurationId(defaultBespokeConfiguration)).toBe(configurationId(defaultBespokeConfiguration));expect(configurationId({...defaultBespokeConfiguration,dial:"charcoal"})).not.toBe(configurationId(defaultBespokeConfiguration));});it("restores only valid session state",()=>{expect(restoreBespokeConfiguration('{"watch":"x"}')).toEqual(defaultBespokeConfiguration);});});
